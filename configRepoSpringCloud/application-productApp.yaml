spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gb
    username: serg
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
